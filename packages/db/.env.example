# Prisma 连接串（docker compose 的 postgres，端口 5433 避免与本机 5432 冲突）
# 注意：用 127.0.0.1 而非 localhost（Prisma 引擎对 localhost 可能走 IPv6 导致 P1001 连不上）
DATABASE_URL="postgresql://engiperf:engiperf@127.0.0.1:5433/engiperf?schema=public"

# seed 数据量
SEED_COUNT=100000